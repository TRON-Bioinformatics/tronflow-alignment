# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: tronflow-bwa-1.5.0
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bioconda::bwa=0.7.17
  - bioconda::samtools=1.12
  - bioconda::fastp=0.20.1